# JavaScript/TypeScript Kubernetes Client information

## Installation

```sh
npm install @carbonql/kubernetes-client-node
```

## Development

To build, **do not use npm directly.** Instead, use `make`:

```sh
$ make dependencies
$ make build
```

## Testing

```console
make test
```

